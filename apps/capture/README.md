# Capture

## Docs
La documentacion de capture app se encuentra en el directorio `docs/`.

## Src
El Front-End de la aplicacion capture se encuentra en el directorio `src/`.

## Src-Tauri
El Back-End de la aplicacion capture se encuentra en el directorio `src-tauri/`

## Target
El directorio `target/` contiene los archivos compilados de la aplicacion capture.


